# Optional: geographic projection + background fetch (static maps, animator).
# Needs requirements-plotting.txt too.
#
# The World file reader `world_reader` (core/load_data/world) is also required
# for maps/rate-per-100k. It is not on PyPI — install it editable from the MAY2
# packaging shim (path is machine-specific, so not pinned here):
#   pip install -e /path/to/MAY2/may_world_visualiser
pyproj
cartopy
pillow
